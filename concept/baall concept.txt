use math.ba; // import

struct: myStructure := (number: field1, string: field2, structure2: field3);

/* block
comment */
// comment

string: a = "GRRRRRR";
number: b = 3;
char: c = 'A';
myStructure: grr := (a,b,c);
number[9]: arr = [1,2,3,4,5];
number[9]: arr2 = []; // array of numbers with size 9

// string{?}: myset = &{1,3,4,5,1};

string: konstante1 := a + b;
number: konstante2 := b + a;

string: function1 := number: a, bool: b, char: c {
	isTrue? {
		funktion2(x, y, z);
		return "Hahaha";
	} | isFalse? {
		// If Else
	} | {
		// Else
	}
	return "Hehehehoar";
}

while a<b {
	// while loop
	break;
}
for i=0..10::i++{
	continue;
}
// equivalent versions
for i=0..10{

}

char: no = none;

exception: myCustomException := (string: message, int epochTime)

raise myCustomException("exception cause", 1230451);

try {
    // run potentially exception raising code
    myFunc();
} intercept myCustomException, DivByZeroException: err {
    // if myCustomException is raised, run stop execution in the attempt block and continue here
    deescalate(err.message);
} ensure {
    // gets executed after either the attempt block or the intercept block finishes successfully
    alwaysExecuteThisFunction("help");
}

export {b,c,arr,arr2, function1}